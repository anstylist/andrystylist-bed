'use strict';

/**
 * newsletter-emails-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::newsletter-emails-list.newsletter-emails-list');
